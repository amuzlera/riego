WIFI_SSID = "TP-Link_7FC4"
WIFI_PASSWORD = "111esp32111"

API_KEY = ""

DEVICE_NAME = "riego-esp32"
HTTP_PORT = 80
WIFI_TIMEOUT_SECONDS = 15
NTP_HOST = "pool.ntp.org"
TZ_OFFSET_SECONDS = 10800

# IP fija que usaba el proyecto viejo.
# Formato: (ip, netmask, gateway, dns)
STATIC_IFCONFIG = (
    "192.168.1.50",
    "255.255.255.0",
    "192.168.1.1",
    "8.8.8.8",
)

# 6 relés / zonas del proyecto anterior.
# active_low=True porque el relé se energiza con 0.
PINS = {
    "zona1": {"pin": 19, "mode": "out", "active_low": True, "default": False},
    "zona2": {"pin": 5, "mode": "out", "active_low": True, "default": False},
    "zona3": {"pin": 18, "mode": "out", "active_low": True, "default": False},
    "zona4": {"pin": 25, "mode": "out", "active_low": True, "default": False},
    "zona5": {"pin": 26, "mode": "out", "active_low": True, "default": False},
    "zona6": {"pin": 27, "mode": "out", "active_low": True, "default": False},
}

SENSORS = {
    "temp_humedad": {
        "kind": "dht11",
        "pin": 4,
    }
}

